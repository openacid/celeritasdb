language: rust

rust:
  - stable
  # - beta
  # - nightly

# matrix:
#   allow_failures:
#     - rust: nightly

script:
  - cargo build --verbose --all
  - cargo test --verbose --all

notifications:
  email:
    on_success: change
    on_failure: always
